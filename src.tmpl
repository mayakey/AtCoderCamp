/**
 * URL:  (URL)
 * BGN: 
 * END: 
*/
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <numeric>
#include <cmath>
#define Rep(i, n) for (int i=0; i<n; i++)
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    return 0;
}
